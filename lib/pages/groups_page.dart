import 'package:flutter/material.dart';

class GroupsPage extends StatelessWidget {
  static const id = 'groups_page';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
